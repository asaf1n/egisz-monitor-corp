# Порядок применения DDL к БД egisz_reports (k8s Job egisz-reports-schema-init, start.ps1, ETL, CLI apply-schema).
# Не включайте сюда 003/004/006 — это ручные диагностические запросы (Firebird / SELECT), не миграции витрины.
001_schema.sql
002_etl_state.sql
005_healthcheck.sql
